 “아래 사양서대로 단일 `index.html` 파일을 만들어 주세요.

 * `intro.html`, `modal.html` 디자인을 한 페이지 안에서 재현하고, 인트로 버튼 클릭 시 모달 카드가 중앙에서 확대되며 유튜브 영상이 자동 재생되게 해주세요.
 * 재생 중에는 항상 ‘홈으로 바로가기’ 버튼을 우측 상단에 노출하고, 클릭 시 `assets/system-notification-199277.mp3` 사운드를 재생한 뒤 인트로로 돌아가세요.
 * 영상이 끝나면 마지막 화면에서 5초 정지 후 자동으로 인트로로 복귀시키세요.
 * YouTube IFrame API를 사용하고, 모바일 자동재생 정책을 고려해 최초 클릭 시점에 `mute()` 후 `playVideo()` 전략을 적용하세요.
 * \*\*4K UHD 세로형 해상도(2160×3840, 9:16)\*\*에 최적화된 레이아웃으로 제작해 주세요.

   * 전체 레이아웃은 세로형 화면을 기준으로 중앙 정렬,
   * 영상 플레이어는 16:9 비율 유지,
   * 버튼/텍스트는 세로 화면에서 보기 좋게 정렬되도록 해주세요.
 * 반응형(9:16 기본, 16:9에서도 대응), 접근성(aria-label, 포커스 관리), 에러 처리, 애니메이션(scale-in/out)을 포함하세요.
 * 테스트 영상 ID는 `M7lc1UVf-VE`를 기본값으로 사용하고, 향후 여러 영상으로 확장 가능하도록 `data-video`와 매핑 구조를 갖추세요.
 * 프로덕션 수준의 깔끔한 CSS/JS 구조로 작성해 주세요.”
